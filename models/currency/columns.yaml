# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        invisible: true

    currency_code:
        label: Code
        searchable: true
        width: 150px

    name:
        label: Name
        searchable: true

    example:
        sortable: false
        type: partial
        label: Example

    is_primary:
        label: Default
        type: switch
        width: 150px

    is_enabled:
        label: Enabled
        type: switch
        invisible: true
        width: 150px
