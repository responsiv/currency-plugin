# ===================================
#  List Column Definitions
# ===================================

columns:
    name:
        label: Name
        searchable: true

    converter_name:
        label: Converter Name

    is_enabled:
        label: Enabled
        type: switch
        width: 10%

    is_default:
        label: Default
        type: switch
        width: 10%