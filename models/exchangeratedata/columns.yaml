# ===================================
#  List Column Definitions
# ===================================

columns:
    rate_value:
        label: Rate Value

    valid_at:
        label: Valid From
        type: date
